---
const {url,description, width, height} = Astro.props;
interface Props{
    url: string;
    description: string;
    width: string;
    height: string;
    }
---
<img class="rounded-2xl md:w-75 lg:w-full" src={url} alt={description} width={width} height={height} />


