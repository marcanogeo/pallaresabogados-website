---
const  { url, description} = Astro.props;
interface props {
    url: string;
    description: string;
}
---
<figure class="relative transition-all duration-300 cursor-pointer mb-4">
    <img src={url} alt={description} class="lg:w-full sm:w-50 max-h-screen sm:h-auto"/>
</figure>

