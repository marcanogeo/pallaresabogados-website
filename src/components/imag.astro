---
const {url,description, width,height} = Astro.props;
interface props{
    url: string;
    description: string;
    width: string;
    height: string;
}
---
<img src={url} alt={description} width={width} height={height} />
